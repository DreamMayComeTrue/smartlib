# Frontend env — copy to `.env` (or `.env.local`) for local dev.
# Vite will inline these into the bundle prefixed with VITE_.

# Base URL of the Slim 4 API served by Laragon
VITE_API_BASE_URL=http://smartlib.test
